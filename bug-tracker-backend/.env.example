# Server
PORT=5000

# MongoDB (use a local instance or MongoDB Atlas)
MONGODB_URI=mongodb://localhost:27017/bug-tracker

# JWT secret for auth tokens (use a long random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Comma-separated origins allowed by CORS (frontend URL)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Frontend URL (for OAuth redirects, e.g. Trello connect)
FRONTEND_URL=http://localhost:3000

# Trello app key (for one-click Connect Trello). Get at https://trello.com/app-key
TRELLO_API_KEY=

# Google OAuth (for Sign in with Google). Create at https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Backend origin for OAuth redirect_uri (e.g. https://api.yourdomain.com). Defaults to localhost:PORT
# BACKEND_ORIGIN=http://localhost:5000

# Lemon Squeezy (recurring subscriptions). Create store + subscription products at https://app.lemonsqueezy.com
# LEMON_SQUEEZY_API_KEY=
# LEMON_SQUEEZY_STORE_ID=
# LEMON_SQUEEZY_VARIANT_PRO=    # variant ID for Pro monthly
# LEMON_SQUEEZY_VARIANT_TEAM=   # variant ID for Team monthly
# LEMON_SQUEEZY_WEBHOOK_SECRET= # signing secret you set when creating the webhook (URL: https://your-api/webhooks/lemonsqueezy)

# Optional: Stripe for paid plans (Pro/Team). Set plan manually in DB until Stripe is connected.
# STRIPE_SECRET_KEY=sk_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_PRICE_PRO_MONTHLY=price_...
